import { AccountForm } from "../components/account-form";

const NewUser = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center">
      <AccountForm title="Register" />
    </main>
  );
};

export default NewUser;
